"use client";

export function PrivacyNotice() {
  return (
    <p className="text-center text-xs text-gray-400">
      We use your GitHub login only to verify a star. We store your username in
      a 24-hour cookie. We never read your repos, your email, or any other data.
      You can disconnect anytime from the workspace header.
    </p>
  );
}
